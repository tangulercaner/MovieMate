export enum MovieType {
  MOVIE = 'movie',
  SERIES = 'series',
  EPISODE = 'episode',
}

export enum RequestSuccess {
  TRUE = 'True',
  FALSE = 'False',
}

export enum LocalStorageKeys {
  favoriteMovies = 'FAVORITE_MOVIES',
}
